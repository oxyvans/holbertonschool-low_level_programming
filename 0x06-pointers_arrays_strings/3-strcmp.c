#include "main.h"

/**
 * _strcmp - comp two strings
 * @s2 : chars
 * @s2 : chars
 *
 * Return: comp
 */

int _strcmp(char *s1, char *s2)
{
int i, j;
for (i = 0; s1[i] != '\0'; i++)
{
}
for (j = 0; s2[j] != '\0'; j++)
{
}
if (i == j)
	return (0);
else
	if (i > j)
		return (15);
	else
		return (-15);
}
