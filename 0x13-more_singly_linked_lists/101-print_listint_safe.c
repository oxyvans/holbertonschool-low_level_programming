#include "lists.h"

/**
 * print_listint_safe - main
 * @head: list.
 *
 * Return: size_t.
 **/

size_t print_listint_safe(const listint_t *head)
{
	size_t cont = 0;

	if (head == NULL)
		exit(98);

	return (cont);
}
