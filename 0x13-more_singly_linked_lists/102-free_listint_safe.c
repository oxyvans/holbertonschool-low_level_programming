#include "lists.h"

/**
 * free_listint_safe - main
 * @h: list.
 *
 * Return: size_t.
 **/

size_t free_listint_safe(listint_t **h)
{
	size_t cont = 0;

	if (h == NULL)
		return (NULL);

	return (cont);
}
