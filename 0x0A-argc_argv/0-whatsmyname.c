#include <stdio.h>
#include <stdlib.h>

/**
 * main - main
 * @argc : size
 * @argv : char
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
if (argc >= 0)
{
	printf("%s\n", argv[0]);
}
return (0);
}
